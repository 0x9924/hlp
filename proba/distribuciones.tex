\documentclass[a4paper,landscape]{article}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{array}
\usepackage{amsmath}
\usepackage[margin=1cm]{geometry}
\newcommand{\tvi}{\vrule height 17pt depth15pt width 0pt} 
\newcolumntype{x}[1]{>{\hfil$\displaystyle} p{#1} <{$\hfil}} 
\begin{document}

%\begin{tabular}{l c c c c}
\begin{tabular}{ x{9em} x{4em} x{15em} x{6em} x{5em} x{15em} x{7em}}
  \hline \\
  \text{Distribucion}
& param.
& p_X(x) \lor f_X(x)
& F_X(x)
& E[X]
& V[X]
& M_X(t)\\[15pt]
  \hline \\
  
  \text{Geom\'etrica}
& G(p)
& (1-p)^{k-1}p
& 1 - (1 - p)^k 
& \frac{1}{p}
& \frac{1-p}{p^2}
& \frac{pe^t}
       {1-(1-p)e^t}
  \\[30pt]
  
  \text{Binomial} 
& B(n,p)
& \binom{n}{k} p^k (1-p)^{n-k}
& \sum_{k = 0}^x p_X(k)
& np
& np(1-p)
& (e^tp+1-p)^n
  \\[30pt]

  \text{Bin} \text{ Neg}
& BN(r,p)
& \binom{k-1}{r-1} p^r (1-p)^{k-r}
& \sum_{k=r}^x p_X(x)
& \frac{r}{p}
& \frac{r(1-p)}{p^2}
& \left(
        \frac{pe^t}
             {1-(1-p)e^t}
  \right)^t
  \\[30pt]

  \text{Hipergeom\'etrica}
& H(n,N,D)
& \frac{\binom{D}{k}\binom{N-D}{n-k}}{\binom{N}{n}}
&
& n\frac{D}{N}
& \left(\frac{N-n}{N-1}\right)n\frac{D}{N}\left(1-\frac{D}{N}\right)
& \\[30pt]

  \text{Poisson}
& Poi(\lambda)
& \frac{e^{-\lambda}\lambda^k}{k!}
&
& \lambda
& \lambda
& \exp{\left(l(e^t-1)\right)}
  \\[30pt]

  \text{Uniforme}
& U[A,B]
& \frac{1}{B-A} \mathbf{1}_{[A,B]}
& \frac{x - A}{B - A}
& \frac{A + B}{2}
& \frac{(B - A)^2}{12}
& \frac{e^{tb} - e^{ta}}
       { t(b-a) }
  \\[30pt]

  \text{Exponencial}
& Exp(\lambda)
& \lambda e^{-\lambda x} \ \mathbf{1}_{\{x > 0\}}
& 1 - e^{-\lambda x}
& \frac{1}{\lambda}
& \frac{1}{\lambda^2}
& \frac{\lambda}{\lambda-t}
  \\[30pt]
  
   \text{Normal}
& \mathcal{N}(\mu,\sigma^2)
& \frac{1}{\sqrt{2\pi\sigma^2}}exp{\left(-\frac{1}{2\sigma^2}(x-\mu)^2\right)}
&
& \mu
& \sigma
& \exp{\left(\frac{\sigma^2 t^2}{2}+\mu t\right)}
  \\[30pt]
  
  \text{Gamma}
& \Gamma(\alpha, \lambda)
& \frac{e^{-\lambda x}x^{\alpha-1}\lambda^\alpha}
       {\Gamma(\alpha)} \ 
  \mathbf{1}_{\{x > 0\}}
& \frac{\alpha}{\lambda}
& \frac{\alpha}{\lambda^2}
&
& \left(\frac{\lambda}{\lambda-t}\right)^\alpha
  \\[15pt]
  
  \hline \\
\end{tabular}




\end{document}